# Deployment

Railway is connected to the `main` branch. Always merge finished work into
`main` and push it — don't leave changes sitting only on a feature branch —
so Railway picks up the latest deploy automatically.
